---
import SiteLayout from '../../layouts/SiteLayout.astro'
import { breadcrumbSchema } from '../../lib/seo-helpers'

const differentiators = [
  { number: '01', title: 'Contextual assessment', seam: 'Requires a Social Impact Assessment for every project — understanding the people, the place, and the issues material to this specific context before any other work begins.', others: 'Prescribe social goals without understanding of community context or what issues are actually material to the project.' },
  { number: '02', title: 'No offsetting of harm', seam: 'Avoiding negative impact must come before earning points for positive initiatives — the scoring design enforces this sequence.', others: 'Allow positive-impact points to offset negative performance — a project can do harm and still earn certification.' },
  { number: '03', title: 'Impact-aligned levels', seam: 'Certification levels align directly to social impact goals — Bronze through Platinum correspond to acting to avoid harm, preventing harm, achieving positive impact, and contributing to lasting change.', others: 'Certification levels based on point thresholds — a project can earn a higher level without demonstrating meaningful improvement for people.' },
  { number: '04', title: 'Leading indicators', seam: 'Measures the principles and processes that cause change, so organizations know they are on the right track before harm is done.', others: 'Measure lagging indicators — outcomes after work is complete. In social issues, that can mean harm has already occurred by the time measurement happens.' },
  { number: '05', title: 'Roadmap design', seam: 'A roadmap with ordered, prerequisite activities, and the guidance to put those activities into practice, to help the work progress with confidence.', others: 'Activities can be completed in any order, often leading to advanced initiatives built on a missing foundation.' },
  { number: '06', title: 'Project-level scope', seam: 'Social initiatives apply to the specific project and the people it touches, with additional recognition for organization-level implementation.', others: 'Organization-wide scope — a heavier lift for organizations early in their social equity journey, and harder to connect to real impact on people in specific places.' },
]

const pillars = [
  {
    number: '01',
    title: 'Social Impact',
    href: '/standard/social-impact',
    description: 'Address social, cultural, and economic effects of your development project before they happen through structured assessments and meaningful community engagement.',
    concepts: ['Impact Assessment: Contextual Assessment, Impacted Party Engagement, Monitoring + Evaluation'],
    icon: '/icons/pillar-social-impact.png',
  },
  {
    number: '02',
    title: 'Social Responsibility',
    href: '/standard/social-responsibility',
    description: 'Strengthen organizational policies, community involvement, and social investment practices to reflect a commitment to responsible development.',
    concepts: ['Transformational Governance (5 objectives)', 'Community Involvement', 'Social Investment'],
    icon: '/icons/pillar-social-responsibility.png',
  },
  {
    number: '03',
    title: 'Social Justice',
    href: '/standard/social-justice',
    description: 'Advance meaningful social sustainability practices across procurement, the workplace, the community, and capital access decision making.',
    concepts: ['Social Equity + Justice (Procurement, Workplace, Community, Capital)', 'Social Justice Innovation'],
    icon: '/icons/pillar-social-justice.png',
  },
  {
    number: '04',
    title: 'Social Accountability',
    href: '/standard/social-accountability',
    description: "Uphold human rights across your project's full value chain through ethical materials and supplier practices, along with health, well-being, and safety standards.",
    concepts: ['Human Rights (Ethical Materials, Ethical Suppliers, Reporting, Education)', 'Health + Safety'],
    icon: '/icons/pillar-social-accountability.png',
  },
]

const layers = [
  { id: 'pillars', title: 'Pillars', count: '4', description: 'The four organizing themes of social equity in the built environment. Every concept, objective, and activity connects back to a pillar so you always understand the why behind your efforts.' },
  { id: 'concepts', title: 'Concepts', count: '8', description: 'Within the pillars, eight focus areas address specific themes from impact assessments and human rights practices to equity in procurement and more.' },
  { id: 'objectives', title: 'Objectives', count: '21', description: 'Measurable objectives define goals within each concept, giving a clear path to progress with rationale and connection to internationally recognized standards.' },
  { id: 'activities', title: 'Activities', count: '50+', description: 'The individual actions that drive equitable change. Each activity has defined requirements, clear scoring rubrics, documentation standards, and implementation instructions so nothing is left to guessing.' },
]

// ─── Orbital framework geometry ───
const VB_W = 800
const VB_H = 720
const CX = VB_W / 2
const CY = VB_H / 2
const R_PILLARS = 95
const R_CONCEPTS = 160
const R_OBJECTIVES = 225

const polar = (r, deg) => {
  const a = ((deg - 90) * Math.PI) / 180
  return { x: +(CX + r * Math.cos(a)).toFixed(2), y: +(CY + r * Math.sin(a)).toFixed(2) }
}

const pillarsGeo = [
  { id: 'p0', angle: 0, label: 'SOCIAL IMPACT' },
  { id: 'p1', angle: 90, label: 'SOCIAL RESPONSIBILITY' },
  { id: 'p2', angle: 180, label: 'SOCIAL JUSTICE' },
  { id: 'p3', angle: 270, label: 'SOCIAL ACCOUNTABILITY' },
]

const conceptsGeo = [
  { id: 'c0', pillar: 'p0', angle: -22 },
  { id: 'c1', pillar: 'p0', angle: 0 },
  { id: 'c2', pillar: 'p0', angle: 22 },
  { id: 'c3', pillar: 'p1', angle: 90 },
  { id: 'c4', pillar: 'p2', angle: 170 },
  { id: 'c5', pillar: 'p2', angle: 190 },
  { id: 'c6', pillar: 'p3', angle: 260 },
  { id: 'c7', pillar: 'p3', angle: 280 },
]

// Distribute 21 objectives across 8 concepts: 3,3,3,3,3,2,2,2
const objectivesGeo = []
const objsPerConcept = [3, 3, 3, 3, 3, 2, 2, 2]
conceptsGeo.forEach((c, i) => {
  const count = objsPerConcept[i]
  const offsets = count === 3 ? [-7, 0, 7] : [-4, 4]
  offsets.forEach((off) => {
    objectivesGeo.push({ id: `o${objectivesGeo.length}`, concept: c.id, angle: c.angle + off })
  })
})

// Distribute 52 activities across 21 objectives: first 10 get 3, remaining 11 get 2 (10*3+11*2=52)
const activitiesGeo = []
objectivesGeo.forEach((o, i) => {
  const count = i < 10 ? 3 : 2
  const aOffsets = count === 3 ? [-3, 0, 3] : [-2, 2]
  const aRadii = count === 3 ? [263, 275, 287] : [268, 283]
  aOffsets.forEach((off, ai) => {
    activitiesGeo.push({ id: `a${activitiesGeo.length}`, objective: o.id, angle: o.angle + off, r: aRadii[ai] })
  })
})

const pillarPos = pillarsGeo.map((p) => ({ ...p, ...polar(R_PILLARS, p.angle) }))
const conceptPos = conceptsGeo.map((c) => ({ ...c, ...polar(R_CONCEPTS, c.angle) }))
const objectivePos = objectivesGeo.map((o) => ({ ...o, ...polar(R_OBJECTIVES, o.angle) }))
const activityPos = activitiesGeo.map((a) => ({ ...a, ...polar(a.r, a.angle) }))

const pById = (id) => pillarPos.find((p) => p.id === id)
const cById = (id) => conceptPos.find((c) => c.id === id)
const oById = (id) => objectivePos.find((o) => o.id === id)

const levels = [
  { name: 'Bronze', tagline: 'Acting to avoid harm', description: 'The project is making measurable progress toward international social targets. Foundational activities are in place. Harm is being identified and addressed.', imp: 'Act to Avoid Harm', logo: '/logos/cert-bronze.png' },
  { name: 'Silver', tagline: 'Preventing harm', description: 'The project has moved from awareness to active prevention — with a demonstrated commitment to growing into positive social impact over time.', imp: 'Act to Avoid Harm (advanced)', logo: '/logos/cert-silver.png' },
  { name: 'Gold', tagline: 'Achieving positive impact', description: 'The project is actively improving the well-being of impacted parties. Social outcomes fall within a sustainable range — real benefit, not just reduced harm.', imp: 'Benefit Impacted Parties', logo: '/logos/cert-gold.png' },
  { name: 'Platinum', tagline: 'Contributing to solutions', description: 'The project is setting the standard for the industry — achieving lasting positive impact at scale, with benefits that extend beyond the project itself.', imp: 'Contribute to Solutions', logo: '/logos/cert-platinum.png' },
]

const levelIntensities = ['bg-seam-700/10', 'bg-seam-700/20', 'bg-seam-800/70', 'bg-seam-900/85']
const levelTextColors = ['text-seam-800', 'text-seam-800', 'text-white', 'text-white']
const levelSubColors = ['text-gold-600', 'text-gold-600', 'text-seam-200', 'text-seam-200']
const levelBodyColors = ['text-warm-600', 'text-warm-600', 'text-seam-100/80', 'text-seam-100/80']
const levelBorderColors = ['border-seam-600/20', 'border-seam-600/20', 'border-seam-400/20', 'border-seam-400/20']

const ratings = [
  { code: 'B+I:D', title: 'Buildings + Interiors: Developer', description: 'Ground-up construction and major renovations where the owner assumes developer responsibilities — project ownership, financing, construction supervision, and tenant leasing management.', href: '/rating-system/buildings-interiors' },
  { code: 'B+I:O', title: 'Buildings + Interiors: Occupier', description: 'Construction or renovation projects where the owner occupies the space — fit-outs, interior buildouts, and workplace environments where the owner is the primary occupant.', href: '/rating-system/buildings-interiors' },
  { code: 'O+M:D', title: 'Operations + Management: Developer', description: 'Existing assets being operated and managed by the developer — applying social equity standards to the ongoing performance and management of a building or portfolio.', href: '/rating-system/operations-maintenance' },
  { code: 'O+M:O', title: 'Operations + Management: Occupier', description: 'Ongoing operations by an occupier — applying social equity practices to the management of an occupied space, including workplace practices, supply chain, and community relationships.', href: '/rating-system/operations-maintenance' },
]

const principles = [
  { number: '01', title: 'Timely contextual analysis', description: 'Assessments are critical for social equity to take root within a project. Because people are complex and communities change, no two projects will be the same. And assessments help shape everything that follows.' },
  { number: '02', title: 'No offsetting of harm', description: 'The scoring design prevents positive-impact points from masking harm. Preventing harm is a prerequisite for earning recognition for positive endeavors — full stop.' },
  { number: '03', title: 'Logic model structure', description: 'Activities follow a causal logic model connecting inputs, processes, outputs, and outcomes in a way that shows the pathway from action to impact.' },
  { number: '04', title: 'Human rights-weighted scoring', description: 'Points are weighted based on the salience of human rights issues — directing organizations to prioritize activities that matter most, not just those that are easiest to complete.' },
  { number: '05', title: 'Roadmap design', description: "Driver activities establish prerequisites for more advanced work. Organizations cannot skip foundational steps — because in social equity, a flawed foundation does not just underperform. It can cause harm." },
  { number: '06', title: 'Impact-aligned levels', description: 'Certification levels reflect where an organization stands on the spectrum of social impact — not just how many points they have accumulated. Level means something because level is defined by what changes for people.' },
]

const audiences = [
  {
    title: 'CRE decision-makers',
    subtitle: 'Developers, owners, architects',
    quote: 'When people feel like they belong in a space, everything performs better — the building, the business, the community.',
    description: 'The SEAM Standard gives you the framework to build for belonging, and the verified recognition to show it. Whether you are managing ESG commitments, responding to investor expectations, or building something worth being proud of.',
    href: '/who-we-serve/developers-owners',
  },
  {
    title: 'Operators + managers',
    subtitle: 'Property managers, asset managers',
    quote: 'The buildings that perform best are the ones where people actually want to be.',
    description: 'SEAM certification helps operators connect social practices to performance outcomes — occupancy, retention, tenant satisfaction, and community relationships. A structured, measurable framework for the work many operators are already doing.',
    href: '/who-we-serve/property-managers',
  },
  {
    title: 'Social equity practitioners',
    subtitle: 'Sustainability, ESG, impact teams',
    quote: 'SEAM gives the work language, structure, and proof — not just intention.',
    description: 'For professionals leading social equity efforts inside real estate organizations, SEAM provides the rigor that makes the work credible — internally and externally. It translates commitment into a framework that can be measured, verified, and communicated.',
    href: '/who-we-serve/impact-professionals',
  },
]
---

<SiteLayout
  title="The SEAM Standard"
  description="The first social equity certification purpose-built for commercial real estate. Four pillars, eight concepts, twenty-one objectives — a rigorous framework that embeds belonging into how buildings are conceived, built, and managed."
  path="/resources/standard"
  jsonLd={breadcrumbSchema([
    { name: 'Home', path: '/' },
    { name: 'Resources', path: '/resources/standard' },
    { name: 'The Standard', path: '/resources/standard' },
  ])}
>

  <!-- Hero -->
  <section class="relative z-20 min-h-[80vh] flex items-center bg-gradient-to-br from-warm-900 via-warm-800 to-seam-900">
    <div class="absolute inset-0 bg-gradient-to-t from-warm-900/80 via-warm-900/40 to-warm-900/20"></div>
    <img
      src="/icons/standard-hero-building.png"
      alt=""
      class="absolute right-[-5%] bottom-0 translate-y-[15%] w-[clamp(560px,75vw,1100px)] pointer-events-none select-none z-30"
    />
    <div class="relative mx-auto max-w-[1400px] px-6 lg:px-10 py-24 lg:py-32 w-full">
      <div class="hero-enter max-w-3xl">
        <span class="inline-block rounded-full bg-seam-600/20 border border-seam-500/30 px-4 py-1.5 text-[13px] font-medium text-seam-300 mb-8">
          The SEAM Standard&trade;
        </span>
        <h1 class="hero-title font-display text-[clamp(2.5rem,5vw,4.5rem)] leading-[1.05] tracking-[-0.04em] text-white max-w-2xl">
          The framework for belonging{' '}
          <em class="font-display italic font-normal text-seam-300">
            in the built environment
          </em>
        </h1>
        <p class="hero-subtitle mt-8 max-w-lg text-[clamp(1.05rem,1.4vw,1.2rem)] leading-relaxed text-warm-300">
          The first social equity certification purpose-built for commercial real estate. A rigorous, human-centered standard that embeds belonging into every stage of how buildings are conceived, built, and managed.
        </p>
        <div class="hero-cta mt-10 flex flex-wrap gap-4">
          <a href="/get-started" class="inline-flex items-center rounded-full bg-white px-8 py-4 text-[16px] font-medium text-warm-900 hover:bg-warm-100 transition-colors duration-300">
            Explore the Standard
          </a>
          <a href="#levels" class="inline-flex items-center rounded-full border border-white/25 px-8 py-4 text-[16px] font-medium text-white hover:border-white/50 transition-colors duration-300">
            View certification levels
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Belief Statement -->
  <section class="py-24 lg:py-32">
    <div class="mx-auto max-w-[1400px] px-6 lg:px-10">
      <div class="reveal-fade-up max-w-4xl mx-auto text-center">
        <blockquote class="font-display text-[clamp(1.5rem,3vw,2.5rem)] leading-[1.3] tracking-[-0.02em] text-warm-900">
          &ldquo;When we account for human impact — from initial design and material sourcing to ongoing operations — everything performs better: the building, the business, the community, the factory worker 1,000 miles away.&rdquo;
        </blockquote>
        <p class="mt-10 text-[17px] leading-relaxed text-warm-600 max-w-2xl mx-auto">
          Equity addresses human impact: in a building, in a community, in the supply chain. It isn't a nice-to-have or an afterthought. It's the foundation — designed into the structure, sourced into the materials, and built into the partnerships throughout the community. The SEAM Standard gives organizations the guidance to do that work with rigor, integrity, and proof.
        </p>
      </div>
    </div>
  </section>

  <!-- Why SEAM -->
  <section class="py-24 lg:py-32 bg-warm-50">
    <div class="mx-auto max-w-[1400px] px-6 lg:px-10">
      <div class="reveal-slide-left max-w-2xl mb-16 lg:mb-20">
        <p class="text-[13px] font-medium uppercase tracking-[0.15em] text-gold-600 mb-6">Why SEAM</p>
        <h2 class="font-display text-[clamp(2rem,4vw,3.5rem)] leading-[1.1] tracking-[-0.03em] text-warm-900">
          Built different.{' '}<em class="italic font-normal text-seam-600">Designed to matter.</em>
        </h2>
        <p class="mt-6 text-[17px] leading-relaxed text-warm-600 max-w-xl">
          SEAM measures the human impact of a building and provides guidance on how to verifiably improve that impact for everyone.
        </p>
      </div>

      <div class="reveal-stagger space-y-4">
        {differentiators.map((d) => (
          <div class="reveal-child rounded-2xl bg-white border border-warm-100 overflow-hidden">
            <div class="grid grid-cols-1 lg:grid-cols-12 gap-0">
              <div class="lg:col-span-1 p-6 lg:p-8 flex items-start">
                <span class="font-display text-[clamp(1.2rem,1.5vw,1.5rem)] tracking-[-0.02em] text-seam-500">{d.number}</span>
              </div>
              <div class="lg:col-span-3 px-6 lg:px-0 lg:py-8">
                <h3 class="font-display text-[20px] lg:text-[22px] tracking-[-0.02em] text-warm-900">{d.title}</h3>
              </div>
              <div class="lg:col-span-4 p-6 lg:p-8 lg:border-l border-warm-100">
                <span class="text-[12px] font-medium uppercase tracking-[0.1em] text-gold-600 mb-2 block">SEAM</span>
                <p class="text-[15px] leading-relaxed text-warm-600">{d.seam}</p>
              </div>
              <div class="lg:col-span-4 p-6 lg:p-8 lg:border-l border-warm-100 bg-warm-50/50">
                <span class="text-[12px] font-medium uppercase tracking-[0.1em] text-warm-600 mb-2 block">Others</span>
                <p class="text-[15px] leading-relaxed text-warm-600">{d.others}</p>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- The Framework -->
  <section class="py-24 lg:py-32">
    <div class="mx-auto max-w-[1400px] px-6 lg:px-10">
      <div class="reveal-slide-left max-w-2xl mb-12">
        <p class="text-[13px] font-medium uppercase tracking-[0.15em] text-gold-600 mb-6">The framework</p>
        <h2 class="font-display text-[clamp(2rem,4vw,3.5rem)] leading-[1.1] tracking-[-0.03em] text-warm-900">
          Four pillars. Eight concepts.{' '}<em class="italic font-normal text-seam-600">One standard.</em>
        </h2>
        <p class="mt-6 text-[17px] leading-relaxed text-warm-600 max-w-xl">
          The SEAM Standard organizes social equity into a structured framework grounded in internationally recognized principles and aligned to the UN Sustainable Development Goals. Each pillar addresses a distinct dimension of social sustainability in the built environment.
        </p>
      </div>

      <div class="reveal-fade-up flex flex-wrap gap-4 sm:gap-8 mb-10 lg:mb-16">
        <div class="flex items-baseline gap-2">
          <span class="font-display text-[32px] tracking-[-0.04em] text-gold-600" data-count-target="4" data-count-suffix="">4</span>
          <span class="text-[14px] text-warm-600">Pillars</span>
        </div>
        <span class="text-warm-200">&middot;</span>
        <div class="flex items-baseline gap-2">
          <span class="font-display text-[32px] tracking-[-0.04em] text-gold-600" data-count-target="8" data-count-suffix="">8</span>
          <span class="text-[14px] text-warm-600">Concepts</span>
        </div>
        <span class="text-warm-200">&middot;</span>
        <div class="flex items-baseline gap-2">
          <span class="font-display text-[32px] tracking-[-0.04em] text-gold-600" data-count-target="21" data-count-suffix="">21</span>
          <span class="text-[14px] text-warm-600">Objectives</span>
        </div>
      </div>

      <div class="reveal-stagger grid grid-cols-1 md:grid-cols-2 gap-6">
        {pillars.map((p) => (
          <a href={p.href} class="reveal-child group relative rounded-2xl border border-warm-100 p-6 sm:p-8 lg:p-12 transition-all duration-300 hover:border-warm-200 hover:shadow-[0_8px_30px_rgba(0,0,0,0.06)] overflow-hidden">
            <img src={p.icon} alt="" class="absolute -top-4 -right-4 w-48 h-48 object-contain opacity-90 pointer-events-none" />
            <span class="relative text-[14px] font-medium text-seam-500 mb-4 block">Pillar {p.number}</span>
            <h3 class="relative font-display text-[24px] lg:text-[28px] tracking-[-0.02em] text-warm-900 mb-4 group-hover:text-gold-600 transition-colors">{p.title}</h3>
            <p class="text-[16px] leading-relaxed text-warm-600 mb-6">{p.description}</p>
            <ul class="space-y-2 border-t border-warm-100 pt-5">
              {p.concepts.map((c) => (
                <li class="flex items-start gap-2.5 text-[14px] leading-snug text-warm-600">
                  <svg class="shrink-0 w-4 h-4 mt-0.5 text-seam-500" fill="none" viewBox="0 0 24 24" stroke-width="2.5" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" d="m4.5 12.75 6 6 9-13.5" />
                  </svg>
                  {c}
                </li>
              ))}
            </ul>
            <span class="inline-block mt-6 text-[14px] font-medium text-gold-600 group-hover:text-gold-700 transition-colors">Explore pillar &rarr;</span>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- How It Works -->
  <section class="py-24 lg:py-32 bg-warm-50">
    <div class="mx-auto max-w-[1400px] px-6 lg:px-10">
      <div class="reveal-slide-left max-w-2xl mb-16 lg:mb-20">
        <p class="text-[13px] font-medium uppercase tracking-[0.15em] text-gold-600 mb-6">How it works</p>
        <h2 class="font-display text-[clamp(2rem,4vw,3.5rem)] leading-[1.1] tracking-[-0.03em] text-warm-900">
          Structured to leave a{' '}<em class="italic font-normal text-seam-600">lasting impact</em>
        </h2>
        <p class="mt-6 text-[17px] leading-relaxed text-warm-600 max-w-xl">
          The SEAM Standard is a layered framework that builds from foundation to impact, with each level informing and enabling the next.
        </p>
      </div>

      <div class="orbital-module grid grid-cols-1 lg:grid-cols-[minmax(0,55%)_minmax(0,45%)] gap-10 lg:gap-12 items-center" data-orbital-module>
        <!-- Orbital diagram -->
        <div class="reveal-scale order-2 lg:order-1">
          <svg
            viewBox={`0 0 ${VB_W} ${VB_H}`}
            class="w-full h-auto max-w-[620px] mx-auto"
            role="img"
            aria-label="SEAM framework: four pillars, eight concepts, twenty-one objectives, and fifty-plus activities arranged as a radial diagram"
          >
            <!-- Tier zone rings (crisp colored bands behind everything) -->
            <g class="orb-zones" aria-hidden="true">
              <circle class="orb-zone" data-zone="pillars" cx={CX} cy={CY} r="88" fill="none" stroke="#3a5c67" stroke-width="79" />
              <circle class="orb-zone" data-zone="concepts" cx={CX} cy={CY} r="160" fill="none" stroke="#4e7685" stroke-width="66" />
              <circle class="orb-zone" data-zone="objectives" cx={CX} cy={CY} r="222" fill="none" stroke="#a3c8d3" stroke-width="59" />
              <circle class="orb-zone" data-zone="activities" cx={CX} cy={CY} r="276" fill="none" stroke="#D78825" stroke-width="48" />
            </g>

            <!-- Guide rings -->
            <g aria-hidden="true">
              <circle cx={CX} cy={CY} r={R_PILLARS} fill="none" stroke="#d9d6cf" stroke-width="1" stroke-dasharray="2 4" />
              <circle cx={CX} cy={CY} r={R_CONCEPTS} fill="none" stroke="#d9d6cf" stroke-width="1" stroke-dasharray="2 4" />
              <circle cx={CX} cy={CY} r={R_OBJECTIVES} fill="none" stroke="#d9d6cf" stroke-width="1" stroke-dasharray="2 4" />
            </g>

            <!-- Activity connection lines (amber) -->
            <g class="orb-lines" data-tier="activities" stroke="#bf7520" stroke-width="0.6" opacity="0.32">
              {activityPos.map((a) => {
                const o = oById(a.objective)
                return <line x1={o.x} y1={o.y} x2={a.x} y2={a.y} />
              })}
            </g>

            <!-- Objective connection lines (teal) -->
            <g class="orb-lines" data-tier="objectives" stroke="#3a5c67" stroke-width="0.8" opacity="0.35">
              {objectivePos.map((o) => {
                const c = cById(o.concept)
                return <line x1={c.x} y1={c.y} x2={o.x} y2={o.y} />
              })}
            </g>

            <!-- Concept connection lines (teal) -->
            <g class="orb-lines" data-tier="concepts" stroke="#3a5c67" stroke-width="1" opacity="0.4">
              {conceptPos.map((c) => {
                const p = pById(c.pillar)
                return <line x1={p.x} y1={p.y} x2={c.x} y2={c.y} />
              })}
            </g>

            <!-- Pillar connection lines (from center) -->
            <g class="orb-lines" data-tier="pillars" stroke="#3a5c67" stroke-width="1.2" opacity="0.45">
              {pillarPos.map((p) => <line x1={CX} y1={CY} x2={p.x} y2={p.y} />)}
            </g>

            <!-- Activity nodes -->
            <g class="orb-nodes" data-tier="activities" fill="#bf7520">
              {activityPos.map((a) => <circle cx={a.x} cy={a.y} r="3" />)}
            </g>

            <!-- Objective nodes -->
            <g class="orb-nodes" data-tier="objectives" fill="#3a5c67">
              {objectivePos.map((o) => <circle cx={o.x} cy={o.y} r="3.5" />)}
            </g>

            <!-- Concept nodes -->
            <g class="orb-nodes" data-tier="concepts" fill="#3a5c67">
              {conceptPos.map((c) => <circle cx={c.x} cy={c.y} r="6" />)}
            </g>

            <!-- Pillar nodes -->
            <g class="orb-nodes" data-tier="pillars" fill="#3a5c67">
              {pillarPos.map((p) => <circle cx={p.x} cy={p.y} r="13" />)}
            </g>

            <!-- Center disk with SEAM wordmark -->
            <g aria-hidden="true">
              <circle cx={CX} cy={CY} r="48" fill="#00313C" />
              <text x={CX} y={CY + 7} text-anchor="middle" font-family="ui-serif, Georgia, 'Times New Roman', serif" font-style="italic" font-size="22" fill="#a3c8d3" letter-spacing="0.06em">SEAM</text>
            </g>

            <!-- Pillar labels -->
            <g fill="#6b6b68" font-size="11" font-weight="600" letter-spacing="0.15em" style="text-transform: uppercase;">
              <text x={CX} y="24" text-anchor="middle">SOCIAL IMPACT</text>
              <text x={VB_W - 10} y={CY + 4} text-anchor="end">SOCIAL RESPONSIBILITY</text>
              <text x={CX} y={VB_H - 10} text-anchor="middle">SOCIAL JUSTICE</text>
              <text x="10" y={CY + 4} text-anchor="start">SOCIAL ACCOUNTABILITY</text>
            </g>
          </svg>
        </div>

        <!-- Linked text tiers -->
        <div class="reveal-stagger order-1 lg:order-2">
          {layers.map((layer) => (
            <div
              class="reveal-child tier-entry group flex gap-6 py-6 lg:py-7 px-4 -mx-4 rounded-xl border-b border-warm-200 cursor-pointer focus:outline-none focus-visible:ring-2 focus-visible:ring-seam-500"
              data-tier-entry
              data-tier={layer.id}
              tabindex="0"
            >
              <div class="shrink-0 w-[60px]">
                <span class="block font-display text-[28px] lg:text-[32px] tracking-[-0.04em] text-gold-600">
                  {layer.count}
                </span>
              </div>
              <div>
                <h3 class="font-display text-[20px] lg:text-[24px] tracking-[-0.02em] text-warm-900 mb-2">{layer.title}</h3>
                <p class="text-[15px] lg:text-[16px] leading-relaxed text-warm-600">{layer.description}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- Certification Levels -->
  <section id="levels" class="py-24 lg:py-32">
    <div class="mx-auto max-w-[1400px] px-6 lg:px-10">
      <div class="reveal-slide-left max-w-2xl mb-16 lg:mb-20">
        <p class="text-[13px] font-medium uppercase tracking-[0.15em] text-gold-600 mb-6">Certification levels</p>
        <h2 class="font-display text-[clamp(2rem,4vw,3.5rem)] leading-[1.1] tracking-[-0.03em] text-warm-900">
          Recognition that reflects{' '}<em class="italic font-normal text-seam-600">real progress</em>
        </h2>
        <p class="mt-6 text-[17px] leading-relaxed text-warm-600 max-w-xl">
          Four levels of SEAM Certification align to the Impact Management Project&apos;s ABCs: avoiding harm, benefiting people, and contributing to solutions.
        </p>
      </div>

      <div class="reveal-stagger grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
        {levels.map((level, i) => (
          <div class={`reveal-child relative rounded-2xl ${levelIntensities[i]} p-8 lg:p-10 overflow-hidden`}>
            <img src={level.logo} alt={`${level.name} certification`} class="absolute -top-4 -right-4 w-28 h-28 object-contain opacity-60 drop-shadow-lg" />
            <h3 class={`relative font-display text-[28px] tracking-[-0.02em] ${levelTextColors[i]}`}>{level.name}</h3>
            <p class={`mt-1 text-[14px] font-medium ${levelSubColors[i]}`}>{level.tagline}</p>
            <p class={`mt-4 text-[15px] leading-relaxed ${levelBodyColors[i]}`}>{level.description}</p>
            <p class={`mt-5 text-[12px] uppercase tracking-[0.1em] ${levelSubColors[i]} border-t ${levelBorderColors[i]} pt-4`}>
              IMP: {level.imp}
            </p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Rating Systems -->
  <section class="py-24 lg:py-32 bg-warm-50">
    <div class="mx-auto max-w-[1400px] px-6 lg:px-10">
      <div class="reveal-slide-left max-w-2xl mb-16 lg:mb-20">
        <p class="text-[13px] font-medium uppercase tracking-[0.15em] text-gold-600 mb-6">Rating systems</p>
        <h2 class="font-display text-[clamp(2rem,4vw,3.5rem)] leading-[1.1] tracking-[-0.03em] text-warm-900">
          Tailored to{' '}<em class="italic font-normal text-seam-600">how you build</em>
        </h2>
        <p class="mt-6 text-[17px] leading-relaxed text-warm-600 max-w-xl">
          Not every activity will apply to every project. SEAM's rating systems ensure that certification reflects your project type and role.
        </p>
      </div>

      <div class="reveal-stagger grid grid-cols-1 md:grid-cols-2 gap-6">
        {ratings.map((r) => (
          <a href={r.href} class="reveal-child group rounded-2xl bg-white p-8 lg:p-10 transition-all duration-300 hover:shadow-[0_8px_30px_rgba(0,0,0,0.06)]">
            <span class="inline-block rounded-lg bg-seam-50 px-3 py-1.5 text-[13px] font-mono font-medium text-gold-600 mb-4">{r.code}</span>
            <h3 class="font-display text-[20px] lg:text-[22px] tracking-[-0.02em] text-warm-900 mb-3 group-hover:text-seam-600 transition-colors">{r.title}</h3>
            <p class="text-[15px] leading-relaxed text-warm-600">{r.description}</p>
            <span class="inline-block mt-4 text-[14px] font-medium text-gold-600 group-hover:text-gold-700 transition-colors">Explore rating system &rarr;</span>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- Framework Design Principles -->
  <section class="py-24 lg:py-32">
    <div class="mx-auto max-w-[1400px] px-6 lg:px-10">
      <div class="reveal-slide-left max-w-2xl mb-16 lg:mb-20">
        <p class="text-[13px] font-medium uppercase tracking-[0.15em] text-gold-600 mb-6">Framework design</p>
        <h2 class="font-display text-[clamp(2rem,4vw,3.5rem)] leading-[1.1] tracking-[-0.03em] text-warm-900">
          Social equity embedded{' '}<em class="italic font-normal text-seam-600">by design</em>
        </h2>
        <p class="mt-6 text-[17px] leading-relaxed text-warm-600 max-w-xl">
          The SEAM Standard does not bolt equity onto a compliance framework. We engineer it into the scoring logic itself in six distinct ways.
        </p>
      </div>

      <div class="reveal-stagger grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
        {principles.map((p) => (
          <div class="reveal-child rounded-2xl border border-warm-100 p-8 lg:p-10">
            <span class="text-[14px] font-medium text-seam-500 mb-3 block">{p.number}</span>
            <h3 class="font-display text-[20px] lg:text-[22px] tracking-[-0.02em] text-warm-900 mb-3">{p.title}</h3>
            <p class="text-[15px] leading-relaxed text-warm-600">{p.description}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Who It's For -->
  <section class="py-24 lg:py-32 bg-warm-50">
    <div class="mx-auto max-w-[1400px] px-6 lg:px-10">
      <div class="reveal-slide-left max-w-2xl mb-16 lg:mb-20">
        <p class="text-[13px] font-medium uppercase tracking-[0.15em] text-gold-600 mb-6">Who it serves</p>
        <h2 class="font-display text-[clamp(2rem,4vw,3.5rem)] leading-[1.1] tracking-[-0.03em] text-warm-900">
          Built for everyone{' '}<em class="italic font-normal text-seam-600">the building touches</em>
        </h2>
      </div>

      <div class="reveal-stagger grid grid-cols-1 lg:grid-cols-3 gap-6">
        {audiences.map((a) => (
          <a href={a.href} class="reveal-child group rounded-2xl bg-white p-10 lg:p-12 transition-all duration-300 hover:shadow-[0_8px_30px_rgba(0,0,0,0.06)]">
            <h3 class="font-display text-[22px] lg:text-[24px] tracking-[-0.02em] text-warm-900 group-hover:text-seam-600 transition-colors">{a.title}</h3>
            <p class="text-[14px] text-warm-600 mt-1 mb-6">{a.subtitle}</p>
            <blockquote class="text-[15px] italic leading-relaxed text-gold-600 border-l-2 border-seam-500 pl-4 mb-6">
              &ldquo;{a.quote}&rdquo;
            </blockquote>
            <p class="text-[15px] leading-relaxed text-warm-600">{a.description}</p>
            <span class="inline-block mt-4 text-[14px] font-medium text-gold-600 group-hover:text-gold-700 transition-colors">Learn more &rarr;</span>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="py-24 lg:py-32 bg-warm-900">
    <div class="reveal-scale mx-auto max-w-[1400px] px-6 lg:px-10 text-center">
      <h2 class="font-display text-[clamp(2rem,4.5vw,4rem)] leading-[1.1] tracking-[-0.03em] text-white max-w-3xl mx-auto">
        Ready to build for{' '}<em class="italic font-normal text-seam-300">belonging?</em>
      </h2>
      <p class="mt-6 text-[17px] text-warm-400 max-w-xl mx-auto">
        Start where you are. The SEAM Standard is designed to meet organizations at their stage of readiness — whether that is exploring what equity means for your portfolio or pursuing full certification today.
      </p>
      <div class="mt-12 flex flex-wrap justify-center gap-4">
        <a href="/certification" class="inline-flex items-center rounded-full bg-white px-8 py-4 text-[16px] font-medium text-warm-900 hover:bg-warm-100 transition-colors duration-300">Start certification</a>
        <a href="/get-started" class="inline-flex items-center rounded-full border border-warm-600 px-8 py-4 text-[16px] font-medium text-warm-300 hover:border-warm-400 hover:text-white transition-colors duration-300">Access the Standard</a>
        <a href="/ap-credential" class="inline-flex items-center rounded-full border border-warm-600 px-8 py-4 text-[16px] font-medium text-warm-300 hover:border-warm-400 hover:text-white transition-colors duration-300">Find a SEAM AP</a>
      </div>
    </div>
  </section>

  <style is:global>
    .orbital-module .orb-nodes circle,
  .orbital-module .orb-lines line,
  .orbital-module .tier-entry {
    transition: opacity 220ms ease, background-color 220ms ease, transform 220ms ease, filter 320ms ease;
  }

  /* Zone rings: faint color wash by default, saturated when active */
  .orbital-module .orb-zone {
    opacity: 0.06;
    transition: opacity 380ms ease;
  }
  .orbital-module[data-active-tier="pillars"] .orb-zone[data-zone="pillars"],
  .orbital-module[data-active-tier="concepts"] .orb-zone[data-zone="concepts"],
  .orbital-module[data-active-tier="objectives"] .orb-zone[data-zone="objectives"],
  .orbital-module[data-active-tier="activities"] .orb-zone[data-zone="activities"] {
    opacity: 0.32;
  }
  /* Non-matching zones recede further */
  .orbital-module[data-active-tier] .orb-zone {
    opacity: 0.025;
  }
  .orbital-module[data-active-tier="pillars"] .orb-zone[data-zone="pillars"],
  .orbital-module[data-active-tier="concepts"] .orb-zone[data-zone="concepts"],
  .orbital-module[data-active-tier="objectives"] .orb-zone[data-zone="objectives"],
  .orbital-module[data-active-tier="activities"] .orb-zone[data-zone="activities"] {
    opacity: 0.32;
  }

  /* Dim non-matching SVG elements when a tier is active */
  .orbital-module[data-active-tier] .orb-nodes,
  .orbital-module[data-active-tier] .orb-lines {
    opacity: 0.18;
  }
  .orbital-module[data-active-tier="pillars"] .orb-nodes[data-tier="pillars"],
  .orbital-module[data-active-tier="pillars"] .orb-lines[data-tier="pillars"],
  .orbital-module[data-active-tier="concepts"] .orb-nodes[data-tier="concepts"],
  .orbital-module[data-active-tier="concepts"] .orb-lines[data-tier="concepts"],
  .orbital-module[data-active-tier="objectives"] .orb-nodes[data-tier="objectives"],
  .orbital-module[data-active-tier="objectives"] .orb-lines[data-tier="objectives"],
  .orbital-module[data-active-tier="activities"] .orb-nodes[data-tier="activities"],
  .orbital-module[data-active-tier="activities"] .orb-lines[data-tier="activities"] {
    opacity: 1;
  }

  /* Also keep inner rings fully visible when an outer tier is active (so the tree reads) */
  .orbital-module[data-active-tier="concepts"] .orb-nodes[data-tier="pillars"],
  .orbital-module[data-active-tier="concepts"] .orb-lines[data-tier="pillars"],
  .orbital-module[data-active-tier="objectives"] .orb-nodes[data-tier="pillars"],
  .orbital-module[data-active-tier="objectives"] .orb-lines[data-tier="pillars"],
  .orbital-module[data-active-tier="objectives"] .orb-nodes[data-tier="concepts"],
  .orbital-module[data-active-tier="objectives"] .orb-lines[data-tier="concepts"],
  .orbital-module[data-active-tier="activities"] .orb-nodes[data-tier="pillars"],
  .orbital-module[data-active-tier="activities"] .orb-lines[data-tier="pillars"],
  .orbital-module[data-active-tier="activities"] .orb-nodes[data-tier="concepts"],
  .orbital-module[data-active-tier="activities"] .orb-lines[data-tier="concepts"],
  .orbital-module[data-active-tier="activities"] .orb-nodes[data-tier="objectives"],
  .orbital-module[data-active-tier="activities"] .orb-lines[data-tier="objectives"] {
    opacity: 0.55;
  }

  /* Text entries: dim non-matching, highlight active */
  .orbital-module[data-active-tier] .tier-entry {
    opacity: 0.4;
  }
  .orbital-module[data-active-tier="pillars"] .tier-entry[data-tier="pillars"],
  .orbital-module[data-active-tier="concepts"] .tier-entry[data-tier="concepts"],
  .orbital-module[data-active-tier="objectives"] .tier-entry[data-tier="objectives"],
  .orbital-module[data-active-tier="activities"] .tier-entry[data-tier="activities"] {
    opacity: 1;
    background-color: rgba(255, 255, 255, 0.7);
  }

  /* Hover affordance on text entries when no tier is active */
  .orbital-module:not([data-active-tier]) .tier-entry:hover {
    background-color: rgba(255, 255, 255, 0.5);
  }

  @media (prefers-reduced-motion: reduce) {
    .orbital-module .orb-nodes circle,
    .orbital-module .orb-lines line,
    .orbital-module .tier-entry {
      transition: none;
    }
  }
</style>

<script is:inline>
  (function () {
    const root = document.querySelector('[data-orbital-module]')
    if (!root) return

    const entries = root.querySelectorAll('[data-tier-entry]')
    const nodeGroups = root.querySelectorAll('.orb-nodes[data-tier]')

    const setTier = (tier) => {
      if (tier) root.setAttribute('data-active-tier', tier)
      else root.removeAttribute('data-active-tier')
    }

    let locked = false

    entries.forEach((entry) => {
      const tier = entry.getAttribute('data-tier')
      entry.addEventListener('mouseenter', () => { if (!locked) setTier(tier) })
      entry.addEventListener('mouseleave', () => { if (!locked) setTier(null) })
      entry.addEventListener('focus', () => setTier(tier))
      entry.addEventListener('blur', () => { if (!locked) setTier(null) })
      entry.addEventListener('click', () => {
        const current = module.getAttribute('data-active-tier')
        if (current === tier && locked) {
          locked = false
          setTier(null)
        } else {
          locked = true
          setTier(tier)
        }
      })
      entry.addEventListener('keydown', (e) => {
        if (e.key === 'Enter' || e.key === ' ') {
          e.preventDefault()
          entry.click()
        }
      })
    })

    nodeGroups.forEach((group) => {
      const tier = group.getAttribute('data-tier')
      group.addEventListener('mouseenter', () => { if (!locked) setTier(tier) })
      group.addEventListener('mouseleave', () => { if (!locked) setTier(null) })
    })

    // Click outside to unlock
    document.addEventListener('click', (e) => {
      if (!locked) return
      if (!root.contains(e.target)) {
        locked = false
        setTier(null)
      }
    })
  })()
</script>

</SiteLayout>
